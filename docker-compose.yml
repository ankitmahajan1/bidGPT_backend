version: '2.1'
services:
  backend:
    image: bidgpt_backend
    depends_on:
      - embedding
    links:
      - embedding
    ports:
      - "8080:8080"
    environment:
      - API_KEY=
  embedding:
    image: embedding_model
    ports:
      - "80:80"